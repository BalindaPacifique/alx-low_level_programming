#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to multiply
 * Return: the result from a mutliplying with b
 */
int mul(int a, int b)
{
	return (a * b);
}
