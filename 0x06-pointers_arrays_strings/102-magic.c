#include <stdio.h>

/**
 * main -  a function to print a[2] = 98
 * -You are not allowed to use the variable a in your new line of code
 * -You can only write one statement
 * -You are not allowed to use ,
 * -You are not allowed to code anything else than the line of code
 * -Your code should be written at line 19, before the ;
 * -Do not remove anything from the initial code
 * -and don’t change anything but the line of code you are adding
 * Return: 0 success, elsee 1 error
 */
int main(void)
{
	int n;
	int a[5];
	int *k;

	a[2] = 1024;
	k = &n;
	*(p + 5) = 98;
	printf("a[2] = %d\n", a[2]);
	return (0);
}
